var temp = prompt("Enter Number");

var results = function (return) {
    return prompt 
}


// Prompt the user to enter a sequence of comma-separated numbers. 
// Your application should take that string of numbers, add them 
// all together and console.log the result.


// Prompt the user to enter a number greater than or equal to 0. Your application 
// should take the number and console.log the factorial of that number.